# 复制一份，改名为 application-local.yml（放在同一个目录），再填上本机 MySQL 的账号密码。
# application-local.yml 已经被 .gitignore 忽略，不会提交到 git。
# 启动时会自动创建 tour_planner 库和表，并导入示例数据，不会动其他库。
spring:
  datasource:
    username: root
    password: 你的MySQL密码
